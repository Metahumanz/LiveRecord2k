# 更新日志

## 0.2.3 - 2026-07-12

- Windows 版不再强制以管理员身份启动，以便目录选择器可以访问和刷新当前用户挂载的网络驱动器。
- 选择录制和导出路径时隐藏 PowerShell 宿主窗口，只显示系统目录/文件选择器。

## 0.2.2 - 2026-07-05

- SC 烧录卡片改为圆角横条，并按内容自动收缩宽度，最长仍限制在合理范围内。
- 礼物横条默认尺寸更醒目、圆角更克制，短礼物会自动变窄，长礼物会在最大宽度内截断。
- 礼物和上船横条保留弹入动效，SC 显示时长统一限制为 5 到 15 秒，避免长时间遮挡画面。
- 烧录导出改为重建视频时间轴并对音频执行异步重采样，降低丢帧或时间戳异常素材导出后音画不同步的概率。
- 纯净片段导出也会先复用源文件修复副本，再从修复后的时间轴剪辑，提高网络波动素材的剪辑稳定性。

## 0.2.1 - 2026-07-05

- 剪辑页新增确认式 H.264 兼容预览代理，HEVC/HVC1 源文件原生预览失败时会先询问再生成，不影响原文件和导出质量。
- 剪辑页首次进入不再默认选中历史素材，导出任务支持排队，正在导出时可以继续选择下一个素材预览和剪辑。
- 路径选择改为系统文件资源管理器，可选择录像保存目录、剪辑输出目录、源视频、弹幕记录和样式文件。
- merged 小分段合并进度显示在直播间卡片里，并增强合并后的残留清理，可清理旧版遗漏的 jsonl、css、ass 和临时文件。
- 烧录前会自动准备修复副本，优先 remux 修复时间戳，必要时转码稳定源文件，降低网络波动后烧录画面撕裂和音画漂移。
- 剪辑页导出烧录片段不再在输出目录额外留下片段字幕文件，只生成用户需要的视频产物。
- 礼物、SC 和上船烧录样式升级为仿直播展示，补全价格、数量、总价和 combo 合并，减少小礼物刷屏遮挡。
- 弹幕显示区域选项明确标注顶部 25%、50%、75% 等比例，TypeScript 配置迁移到新的模块解析方式以消除编辑器弃用诊断。

## 0.2.0 - 2026-07-04

- 剪辑导出和弹幕视频生成进度新增预计剩余时间，并显示本次使用的软件或硬件编码器。
- 剪辑页新增用当前播放位置快速设置入点和出点的图标按钮，MP4 等本地视频预览失败时会显示明确原因。
- 录制配置保存后新增绿色非阻塞成功提示，带关闭按钮和自动消失进度条。
- 左侧导航栏支持收起和展开，底部状态指标固定在导航栏底部，不再随长页面滚动。
- 直播间卡片移除底部弹幕生成控件，主播名旁新增房间号，常用录制操作更聚焦。
- 弹幕版编码会在检测到可用硬件编码时优先选择硬编，设置和维护页会展示当前编码、可用硬编和不可用原因。
- HLS 录制和实时预览增强断线恢复，录制中临时文件长时间不增长时会自动重连直播流。
- 设置页打开目录按钮改为打开当前输入的录像保存目录，避免保存目录和打开目录不一致。

## 0.1.9 - 2026-07-04

- 弹幕烧录新增与 B 站直播对齐的显示区域选项，支持 `1/4屏`、`半屏`、`3/4屏`、`不重叠` 和 `不限`。
- 礼物和舰长事件改为缩小后的左下圆角条向上滚动展示，密集互动会排队进入，避免互相遮挡。
- 录制配置页拆出软件维护页，运行信息、配置备份和版本更新不再挤在同一个设置页面里。
- 总览页“下一步”提示新增不再提示开关，可在录制配置里重新打开。
- GitHub Release 附件精简为安装器、完整运行包和 `update.json`，不再单独发布托盘和后台服务 exe。

## 0.1.8 - 2026-07-03

- 总览页活动直播间卡片移除“生成字幕”和“生成弹幕视频”入口，只保留刷新、监听和录制等常用操作。
- 新录制按“直播间号-主播名称 / 每场直播”两级子文件夹保存，同一场直播的分段、续录和合并产物会留在同一目录。
- 录制中修改保存目录、输出容器或分段时长时，当前整场录制继续使用开场时的配置，新设置从下一场录制开始生效。
- 分段或断流续录合并成功后会从录像库移除小分段，并清理源视频、弹幕、字幕和碎片烧录产物。
- 录像库刷新改为递归扫描保存目录，兼容新分层目录和旧版平铺录像。

## 0.1.7 - 2026-07-03

- 应用内更新下载完成后不再自动执行安装器，改为提示打开下载目录后手动安装，降低被 Defender/SmartScreen 拦截的风险。
- 更新提示会明确说明安装会中断监听和录制，避免用户在任务进行中误触安装。
- 录制/烧录进行中只允许排队下载更新包，任务结束后仍需用户手动运行安装器。
- 更新器移除通过 PowerShell `EncodedCommand` 提权启动安装器的路径，默认安装参数也不再强制追加 `/S` 静默安装。

## 0.1.6 - 2026-07-02

- HLS/fMP4 录制不再使用会卡住分片 EOF 的重连参数，避免开播后有流但临时文件不增长或最终文件为空。
- 实时预览优先选择浏览器可播放的 HLS 流，临时 MKV 预览文案明确说明录制中的 MKV 可能无法被浏览器直接播放。
- 录制时会周期性重新检查目标清晰度，开播初期只有 1080P、稍后出现 2K 时会自动切到更高清晰度的新分段。
- 直播间卡片修复直播状态和录制状态同时显示“录制中”的重复标签。
- 剪辑导出和弹幕视频生成会识别短录像被容器误报为分段上限时长的情况，进度条和默认出点会使用更可信的实际时长。

## 0.1.5 - 2026-07-02

- 剪辑导出和弹幕视频生成会重新探测录像真实时长，短于分段时间的录像不再按 60 分钟显示进度或默认出点。
- 弹幕视频生成和剪辑导出新增中断入口，取消后会停止 ffmpeg 并清理未完成的输出文件。
- WebUI 新增监听地址设置，可在可信局域网中切换为 `0.0.0.0`，让其它电脑通过本机局域网 IP 访问。
- 设置导入导出会保留监听地址，托盘和运行信息会显示当前实际监听地址和端口。
- 修复默认更新清单失败后判断 fallback 时引用未定义变量的问题，检查更新不再报 `DEFAULT_UPDATE_MANIFEST_URL is not defined`。

## 0.1.4 - 2026-07-02

- 前端首页改为新手引导式总览，会按登录、输出目录、直播间、监听和直播状态提示下一步操作。
- 直播间页面新增添加房间、刷新状态、监听或录制的三步说明，房间卡片只突出常用状态和操作。
- 房间卡片将清晰度、接口 qn、实际分辨率、弹幕通道和输出路径等信息收进技术详情，降低新手误读成本。
- 设置页按开始使用、录制质量、弹幕视频、通知和启动、高级维护重新组织，并补充关键设置说明。
- 剪辑导出、日志页和 README 改为用户优先说明，减少装饰性小字和工程细节前置。

## 0.1.3 - 2026-06-30

- 项目源码整理为 `src/client` 和 `src/server` 两个入口，明确前端 React/Vite 与本地 Node 后端的边界。
- 后端入口拆为启动、路由、服务门面和弹幕/ASS/ffmpeg/shared 工具模块，降低录制、预览、更新逻辑继续堆在单文件里的风险。
- 前端拆出页面、通用组件、房间组件、类型、工具函数和分组 CSS，`App.tsx` 回归到应用骨架和全局状态装配。
- `npm run build` 成功后会在 Windows 资源管理器中选中生成的安装器，发布包位置更容易确认；自动打开可通过 `BUILD_NO_OPEN=1` 关闭。
- README 新增项目结构说明，明确 `server` 只是 Node 后端源码目录，不是单独项目或另一套语言栈。

## 0.1.2 - 2026-06-30

- 剪辑页字幕生成和烧录导出严格跟随时间轴选区，烧录片段会重置时间戳以保持视频、音频和弹幕对齐。
- 实时预览弹窗新增临时 MKV 模式，可打开正在写入的当前分段文件，并在分段切换或刷新时重新加载当前临时文件。
- 弹幕版编码器改为按本机 ffmpeg 和显卡环境探测，只展示实际可用的软件、NVENC、QSV 或 AMF 选项。
- 烧录弹幕版和剪辑导出新增 ffmpeg 进度条，按处理时间显示百分比、完成和失败状态。
- 设置页改为稳定的区域布局，录制参数、更新信息和运行路径对齐展示，减少长短面板错位。
- Windows 托盘启动器启动时会请求管理员权限，后台服务继承提权后再启动，降低后续拉起更新安装器失败的概率。

## 0.1.1 - 2026-06-30

- 应用内更新改为通过提权安装器执行，安装器会写入更新状态和日志，安装完成后新版服务会清理已下载的安装包。
- 直播间卡片新增点开式实时预览播放器，通过本地 HLS 代理播放直播流，不再把关键帧图片当成实时画面。
- 录制状态机新增启动锁和续录等待状态，避免 MP4 封装或异常 EOF 续录间隙被监听轮询重复触发开播自动录制。
- 卡片画面文案改为预览图，实时观看入口独立为播放器按钮，降低关键帧刷新和直播预览之间的误解。

## 0.1.0 - 2026-06-29

- 分段录制切换到下一小时后，状态接口优先从活跃录制会话生成当前文件、弹幕和封装状态，避免卡片停留在上一段的最终文件。
- 分段继续录制会复用上一段直播流并先启动下一段，再后台封装上一段 MP4，减少分段边界的写入空窗。
- 剪辑页时间轴改为单条进度条上的入点、播放点和出点三标尺，减少冗余控件并提升拖动选段效率。
- 刷新历史录像会按当前输出目录重建录像库并移除不存在的旧记录，剪辑页不再保留已移走文件作为可选源。
- 设置页新增设置导入和导出 JSON，便于备份输出目录、清晰度、通知、更新源和登录凭证等配置。
- 打包流程会自动识别用户目录下安装的 NSIS，安装器日志改为 ASCII 文本以避免中文乱码。

## 0.0.8 - 2026-06-29

- 发布流程新增 NSIS 安装器，应用内更新改为下载并启动安装器，不再用 PowerShell 自替换文件。
- 实时画面模式会静默刷新直播间信息并穿透图片缓存，减少卡片画面停在旧关键帧的问题。
- 分段录制新增 ffmpeg 时长限制和进程退出兜底，降低 60 分钟分段拖成长临时 MKV 的概率。

## 0.0.7 - 2026-06-29

- 本地 API 新增同源来源校验，阻止其它网页跨站调用本机录播服务。
- 图片代理只对 B 站相关域名携带登录 Cookie 和 Referer，并限制远端图片体积。
- 更新流程新增仅下载更新包入口，立即更新会复用已下载且校验通过的安装包。
- 排队更新时禁用重复下载操作，避免录制结束后自动更新被意外取消。
- 修复手机窄屏下页面被 760px 最小宽度撑开的横向滚动问题。
- 请求体错误返回更明确的 400/413 状态，降低排查 API 问题时的误导。

## 0.0.6 - 2026-06-29

- 明确默认 MP4 是最终输出容器，录制中先写 `.recording.mkv`，结束后再封装为 `.clean.mp4`。
- 直播间卡片新增录制封装阶段、临时文件路径、请求清晰度、接口 qn 和实际写入分辨率展示。
- 更新检查新增前后端超时、连接重试和 GitHub Release API fallback，失败时显示更新日志与状态文件路径。
- 弹幕统计拆分为可烧录事件、原始互动包、未烧录包和命令摘要，减少“弹幕事件 0”的误判。
- 剪辑页显示无效历史文件原因，新增选区条、用当前播放点设起止点和导出后打开所在目录。
- 更新 README 中 MP4、清晰度优先请求和弹幕诊断相关说明。

## 0.0.5 - 2026-06-28

- 默认 MP4 录制改为先写 `.recording.mkv` 临时文件，结束后再封装为 `.clean.mp4`，避免直播中断留下 28 字节空 MP4。
- 录制阶段只映射视频和可选音频，过滤未知流、字幕和 data 流，降低 MP4 损坏概率。
- 录制失败时记录临时路径、临时文件大小、ffmpeg 退出码和最后 stderr，方便定位没有生成文件的真实原因。
- 更新下载新增进度条、临时网络错误自动重试，并在连接中断时显示重试状态。
- 更新应用阶段新增脚本日志、状态文件和 45 秒看门狗，避免长期停在“正在应用更新”。
- 修复直播间卡片封面和状态信息布局，避免 badge 被实时画面遮挡。

## 0.0.4 - 2026-06-28

- 增强直播流续录和异常 EOF 处理，支持自动合并相邻续录片段并合并弹幕事件时间轴。
- 录制结束后校验文件体积和视频流信息，跳过过小或无效录像，并对 MP4 做收尾处理以提升可播放性。
- 停止录制和服务退出时更可靠地结束 ffmpeg，避免没有烧录任务时仍残留进程。
- 更新检查和下载支持环境变量、Windows WinINET 与 WinHTTP 代理配置。
- 实时画面改为更积极的 5 秒刷新，并穿透浏览器和远端图片缓存。
- 剪辑页升级为历史录像列表、视频预览和可拖动时间轴，支持扫描输出目录里的旧录像。
- 设置页新增当前版本、更新状态和更新日志展示。

## 0.0.3 - 2026-06-28

- 新增弹幕样式 CSS 生成链路，烧录前先生成 `.danmaku.css` 和 `.ass`，方便手动调整字幕样式后再烧录。
- 新增只生成字幕文件选项，可只导出 CSS/ASS 而不启动视频烧录。
- 新增弹幕版内容选择，支持“仅弹幕”和“弹幕和礼物”，礼物模式包含 SC、礼物和舰长事件。
- 新增剪辑导出页面，支持选择最近录像或手填路径，按起止时间导出纯净片段或烧录片段。
- 纯净片段导出使用源流复制，烧录片段会先生成片段字幕再只转码选中时间段。

## 0.0.2 - 2026-06-28

- 优化高画质拉流，按 4K/2K/原画等清晰度逐级探测，并在未拿到目标画质时提示登录或刷新 Cookie。
- 修复登录后弹幕连接，补齐 WBI 签名、弹幕通道状态、热度和互动统计显示。
- 新增直播间卡片封面/实时画面切换，补充图片代理，减少 B 站图片防盗链导致的空白。
- 支持按自定义时长分段录制，默认每 60 分钟切分一个 MP4 文件。
- 新增检查更新、立即更新、录制结束后更新和配置目录入口。
- 新增 GitHub Actions 标签发布流程，推送 `v*` 标签时自动打包 Windows 发布包和热更新清单。

## 0.0.1 - 2026-06-28

- 初始发布本地 WebUI 版哔哩录播 2K。
- 支持扫码登录、直播间监听、源流录制、弹幕事件采集和弹幕版烧录。
- 默认 MP4 输出，支持 H.265 优先拉流和弹幕版编码器配置。
- 提供 Windows 托盘入口、后台静默运行、右键菜单、系统通知、开机自启和自定义端口。
- 修复弹幕版编码器设置保存、输出目录打开和托盘悬浮重复显示问题。
