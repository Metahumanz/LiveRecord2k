#!/bin/sh
set -eu
if command -v systemctl >/dev/null 2>&1; then
  systemctl daemon-reload || true
fi
if [ "${1:-}" = purge ]; then
  echo 'BiliRecord2K recordings and /etc/bili-record-2k were preserved intentionally.'
fi
exit 0
